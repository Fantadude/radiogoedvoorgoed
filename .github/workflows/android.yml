name: Android CI

on:
  push:
    branches: [ main, develop ]
    paths:
      - 'app/**'
      - 'gradle/**'
      - 'build.gradle.kts'
      - 'settings.gradle.kts'
      - 'gradle.properties'
      - 'gradlew'
      - 'gradlew.bat'
      - '.github/workflows/android.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'app/**'
      - 'gradle/**'
      - 'build.gradle.kts'
      - 'settings.gradle.kts'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Run tests
      run: ./gradlew test

    - name: Build debug APK
      run: ./gradlew assembleDebug

    - name: Upload debug APK
      uses: actions/upload-artifact@v4
      with:
        name: radiogoedvoorgoed-debug-apk
        path: app/build/outputs/apk/debug/app-debug.apk
        if-no-files-found: error

    - name: Upload APK to GitHub Releases (on main branch only)
      if: github.ref == 'refs/heads/main'
      uses: softprops/action-gh-release@v2
      with:
        files: app/build/outputs/apk/debug/app-debug.apk
        tag_name: v${{ github.run_number }}
        name: Debug Build ${{ github.run_number }}
        body: |
          Automated debug build from commit ${{ github.sha }}
          
          **Features:**
          - MediaSession for lock screen controls
          - Foreground service for background playback
          - Bluetooth media button support
          - Notification with play/pause controls
          
          Download the APK below to test on your Android device.
        draft: true
        prerelease: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Uncomment to build release (requires signing setup)
  # build-release:
  #   needs: build
  #   runs-on: ubuntu-latest
  #   if: github.ref == 'refs/heads/main'
  #   steps:
  #   - name: Checkout
  #     uses: actions/checkout@v4
  #   
  #   - name: Set up JDK 17
  #     uses: actions/setup-java@v4
  #     with:
  #       java-version: '17'
  #       distribution: 'temurin'
  #       cache: gradle
  #   
  #   - name: Grant execute permission for gradlew
  #     run: chmod +x gradlew
  #   
  #   - name: Build release APK
  #     run: ./gradlew assembleRelease
  #   
  #   - name: Sign release APK
  #     uses: r0adkll/sign-android-release@v1
  #     with:
  #       releaseDirectory: app/build/outputs/apk/release
  #       signingKeyBase64: ${{ secrets.SIGNING_KEY }}
  #       alias: ${{ secrets.ALIAS }}
  #       keyStorePassword: ${{ secrets.KEY_STORE_PASSWORD }}
  #       keyPassword: ${{ secrets.KEY_PASSWORD }}
 #   
  #   - name: Upload release APK
  #     uses: actions/upload-artifact@v4
  #     with:
  #       name: release-apk
  #       path: app/build/outputs/apk/release/*.apk
